query search(
    $query: String!,
) {
    search(type: USER, query: $query, first: 10) {
        nodes {
            ... on User {
                email
                login
                name
                avatarUrl
            }
        }
    }
}